# Architecture Technique – Sweet Narcisse

Documentation détaillée de l'architecture système, des flux de données et des composants techniques de la plateforme Sweet Narcisse.

---

## 📋 Table des Matières

- [Vue d'Ensemble](#vue-densemble)
- [Architecture Système](#architecture-système)
- [Flux de Données](#flux-de-données)
- [Composants Techniques](#composants-techniques)
- [Modèle de Données](#modèle-de-données)
- [Intégrations](#intégrations)
- [Sécurité](#sécurité)
- [Déploiement](#déploiement)

---

## Vue d'Ensemble

Sweet Narcisse est une application full-stack moderne construite avec Next.js, offrant une expérience utilisateur fluide pour la réservation de promenades en barque.

### Principes Architecturaux

| Principe | Description |
|----------|-------------|
| **Monorepo** | Frontend, backend et mobile dans un seul dépôt |
| **Server-First** | React Server Components par défaut |
| **Type-Safe** | TypeScript strict, validation Zod |
| **API-First** | Endpoints REST documentés OpenAPI |
| **Mobile-Ready** | PWA + applications natives Capacitor |

---

## Architecture Système

### Diagramme Global

```
                                    ┌─────────────────┐
                                    │    Internet     │
                                    └────────┬────────┘
                                             │
                                    ┌────────▼────────┐
                                    │   Cloudflare    │
                                    │  (CDN + WAF)    │
                                    └────────┬────────┘
                                             │
                              ┌──────────────┼──────────────┐
                              │              │              │
                     ┌────────▼────────┐     │     ┌───────▼───────┐
                     │   Web Client    │     │     │ Mobile Client │
                     │   (Browser)     │     │     │ (Android/iOS) │
                     └────────┬────────┘     │     └───────┬───────┘
                              │              │              │
                              └──────────────┼──────────────┘
                                             │
                                    ┌────────▼────────┐
                                    │     Nginx       │
                                    │ (Reverse Proxy) │
                                    │   Port 80/443   │
                                    └────────┬────────┘
                                             │
                                    ┌────────▼────────┐
                                    │   Next.js App   │
                                    │   (Docker)      │
                                    │   Port 3000     │
                                    └────────┬────────┘
                                             │
              ┌──────────────┬───────────────┼───────────────┬──────────────┐
              │              │               │               │              │
     ┌────────▼────────┐ ┌───▼───┐ ┌────────▼────────┐ ┌────▼────┐ ┌───────▼───────┐
     │   PostgreSQL    │ │ Redis │ │     MinIO       │ │ Stripe  │ │    Resend     │
     │   (Database)    │ │(Cache)│ │   (Storage)     │ │(Payment)│ │   (Email)     │
     │   Port 5432     │ │ 6379  │ │   Port 9000     │ │  API    │ │     API       │
     └─────────────────┘ └───────┘ └─────────────────┘ └─────────┘ └───────────────┘
```

### Architecture Applicative

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              NEXT.JS APPLICATION                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                        PRESENTATION LAYER                            │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │    │
│  │  │   Pages     │  │  Layouts    │  │ Components  │  │   Modals    │ │    │
│  │  │  (app/)     │  │  (app/)     │  │(components/)│  │             │ │    │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                    │                                         │
│                                    ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                          API LAYER                                   │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │    │
│  │  │  REST API   │  │  Auth API   │  │ Webhooks    │  │  Mobile API │ │    │
│  │  │ (app/api/)  │  │(NextAuth)   │  │(stripe,etc.)│  │(app/api/m/) │ │    │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                    │                                         │
│                                    ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                        BUSINESS LAYER                                │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │    │
│  │  │  Booking    │  │  Payment    │  │   Fleet     │  │   Admin     │ │    │
│  │  │  Service    │  │  Service    │  │  Service    │  │  Service    │ │    │
│  │  │  (lib/)     │  │  (lib/)     │  │  (lib/)     │  │  (lib/)     │ │    │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                    │                                         │
│                                    ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                         DATA LAYER                                   │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │    │
│  │  │   Prisma    │  │    Redis    │  │   MinIO     │  │  External   │ │    │
│  │  │    ORM      │  │   Client    │  │   Client    │  │    APIs     │ │    │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Flux de Données

### Flux de Réservation Client

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Client  │     │  Widget  │     │   API    │     │ Database │     │  Stripe  │
│(Browser) │     │(React)   │     │(Next.js) │     │(Postgres)│     │  (Pay)   │
└────┬─────┘     └────┬─────┘     └────┬─────┘     └────┬─────┘     └────┬─────┘
     │                │                │                │                │
     │ 1. Select Date │                │                │                │
     │───────────────>│                │                │                │
     │                │ 2. GET /api/availability        │                │
     │                │───────────────>│                │                │
     │                │                │ 3. Query slots │                │
     │                │                │───────────────>│                │
     │                │                │<───────────────│                │
     │                │<───────────────│                │                │
     │<───────────────│                │                │                │
     │                │                │                │                │
     │ 4. Select Slot │                │                │                │
     │───────────────>│                │                │                │
     │                │                │                │                │
     │ 5. Enter Info  │                │                │                │
     │───────────────>│                │                │                │
     │                │ 6. POST /api/bookings (PENDING) │                │
     │                │───────────────>│                │                │
     │                │                │ 7. Create booking               │
     │                │                │───────────────>│                │
     │                │                │<───────────────│                │
     │                │                │ 8. Create PaymentIntent         │
     │                │                │───────────────────────────────>│
     │                │                │<───────────────────────────────│
     │                │<───────────────│                │                │
     │<───────────────│                │                │                │
     │                │                │                │                │
     │ 9. Enter Card  │                │                │                │
     │───────────────>│                │                │                │
     │                │ 10. Confirm (Stripe.js)         │                │
     │                │───────────────────────────────────────────────>│
     │                │<───────────────────────────────────────────────│
     │                │                │                │                │
     │                │                │ 11. Webhook: payment_intent.succeeded
     │                │                │<───────────────────────────────│
     │                │                │ 12. Update booking → CONFIRMED │
     │                │                │───────────────>│                │
     │                │                │ 13. Send confirmation email     │
     │                │                │─────────────────────────────>  │
     │<───────────────│                │                │                │
     │  14. Success   │                │                │                │
     │                │                │                │                │
```

### Flux d'Embarquement Mobile

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ Employee │     │  Mobile  │     │   API    │     │ Database │     │  Stripe  │
│(Android) │     │   App    │     │(Next.js) │     │(Postgres)│     │ Terminal │
└────┬─────┘     └────┬─────┘     └────┬─────┘     └────┬─────┘     └────┬─────┘
     │                │                │                │                │
     │ 1. Scan QR     │                │                │                │
     │───────────────>│                │                │                │
     │                │ 2. POST /api/mobile/bookings/verify              │
     │                │───────────────>│                │                │
     │                │                │ 3. Validate token               │
     │                │                │───────────────>│                │
     │                │                │<───────────────│                │
     │                │<───────────────│ 4. Booking details              │
     │<───────────────│                │                │                │
     │                │                │                │                │
     │ 5. Tap to Pay  │                │                │                │
     │───────────────>│                │                │                │
     │                │ 6. POST /api/payments/terminal/session           │
     │                │───────────────>│                │                │
     │                │                │ 7. Create session               │
     │                │                │───────────────>│                │
     │                │<───────────────│                │                │
     │                │                │                │                │
     │                │ 8. Initialize Terminal SDK      │                │
     │                │───────────────────────────────────────────────>│
     │                │<───────────────────────────────────────────────│
     │ 9. Tap Card    │                │                │                │
     │───────────────>│                │                │                │
     │                │ 10. Process payment             │                │
     │                │───────────────────────────────────────────────>│
     │                │<───────────────────────────────────────────────│
     │                │ 11. POST /api/payments/terminal/session/confirm │
     │                │───────────────>│                │                │
     │                │                │ 12. Update booking → CONFIRMED │
     │                │                │───────────────>│                │
     │<───────────────│                │                │                │
     │  13. Success   │                │                │                │
```

### Flux Administrateur

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Admin   │     │  Admin   │     │   API    │     │ Database │
│(Browser) │     │   UI     │     │(Next.js) │     │(Postgres)│
└────┬─────┘     └────┬─────┘     └────┬─────┘     └────┬─────┘
     │                │                │                │
     │ 1. Login       │                │                │
     │───────────────>│                │                │
     │                │ 2. POST /api/auth/signin        │
     │                │───────────────>│                │
     │                │                │ 3. Verify credentials
     │                │                │───────────────>│
     │                │                │<───────────────│
     │                │<───────────────│ 4. Session token
     │<───────────────│                │                │
     │                │                │                │
     │ 5. View Planning                │                │
     │───────────────>│                │                │
     │                │ 6. GET /api/admin/all-bookings  │
     │                │───────────────>│                │
     │                │                │ 7. Query (with auth check)
     │                │                │───────────────>│
     │                │                │<───────────────│
     │                │<───────────────│                │
     │<───────────────│                │                │
     │                │                │                │
     │ 8. Modify Booking               │                │
     │───────────────>│                │                │
     │                │ 9. PATCH /api/bookings/[id]     │
     │                │───────────────>│                │
     │                │                │ 10. Auth + Update
     │                │                │───────────────>│
     │                │                │<───────────────│
     │                │<───────────────│ 11. Log action │
     │<───────────────│                │                │
```

---

## Composants Techniques

### Structure des Dossiers

```
sweet-narcisse/
├── app/                          # Next.js App Router
│   ├── [lang]/                   # Pages multilingues (client)
│   │   ├── page.tsx              # Page d'accueil
│   │   ├── layout.tsx            # Layout avec i18n
│   │   ├── cgv/                  # CGV
│   │   ├── legal/                # Mentions légales
│   │   └── privacy/              # Politique de confidentialité
│   │
│   ├── admin/                    # Back-office
│   │   ├── page.tsx              # Dashboard
│   │   ├── planning/             # Planning des réservations
│   │   ├── reservations/         # Gestion réservations
│   │   ├── fleet/                # Gestion flotte
│   │   ├── employees/            # Gestion employés
│   │   ├── accounting/           # Comptabilité
│   │   ├── stats/                # Statistiques
│   │   ├── cms/                  # Gestion contenu
│   │   └── settings/             # Paramètres
│   │
│   ├── api/                      # API Routes
│   │   ├── auth/                 # Authentification NextAuth
│   │   ├── bookings/             # CRUD réservations
│   │   ├── admin/                # Endpoints admin
│   │   ├── mobile/               # API mobile
│   │   ├── payments/             # Paiements Stripe/PayPal
│   │   └── webhooks/             # Webhooks externes
│   │
│   └── login/                    # Page de connexion
│
├── components/                   # Composants React
│   ├── BookingWidget.tsx         # Widget de réservation
│   ├── PaymentElementWrapper.tsx # Intégration Stripe
│   ├── PayPalButton.tsx          # Bouton PayPal
│   └── emails/                   # Templates email React
│
├── lib/                          # Logique métier
│   ├── prisma.ts                 # Client Prisma singleton
│   ├── redis.ts                  # Client Redis
│   ├── storage.ts                # Client MinIO
│   ├── mailer.ts                 # Service email
│   ├── stripe.ts                 # Intégration Stripe
│   ├── paypal.ts                 # Intégration PayPal
│   ├── availability.ts           # Calcul disponibilités
│   ├── adminAccess.ts            # Guards d'autorisation
│   └── payments/                 # Services paiement
│       ├── stripeTerminal.ts     # Stripe Terminal SDK
│       └── paymentSessions.ts    # Gestion sessions
│
├── prisma/                       # Base de données
│   ├── schema.prisma             # Schéma BDD
│   ├── migrations/               # Migrations
│   └── seed.ts                   # Données initiales
│
├── android/                      # Application Android native
│   └── app/
│       └── src/main/java/
│           └── fr/sweetnarcisse/
│               ├── MainActivity.java
│               ├── PaymentActivity.java
│               └── PaymentPollingService.java
│
├── dictionaries/                 # Traductions i18n
│   ├── fr.json
│   ├── en.json
│   ├── de.json
│   ├── es.json
│   └── it.json
│
├── monitoring/                   # Stack monitoring
│   ├── prometheus.yml
│   ├── alertmanager.yml
│   └── grafana/
│
├── nginx/                        # Configuration reverse proxy
│   └── nginx.conf
│
├── scripts/                      # Scripts utilitaires
│   ├── setup-dev-https.ps1
│   ├── configure-env.sh
│   └── harden-vps.sh
│
└── docker-compose.yml            # Orchestration containers
```

### Technologies par Composant

| Composant | Technologies | Rôle |
|-----------|-------------|------|
| **Frontend Web** | Next.js 16, React 19, Tailwind CSS | Interface utilisateur |
| **API Backend** | Next.js API Routes, Zod | Logique serveur |
| **ORM** | Prisma 5.17 | Abstraction BDD |
| **Base de données** | PostgreSQL 16 | Persistance |
| **Cache** | Redis (Upstash) | Sessions, rate limiting |
| **Stockage fichiers** | MinIO | Documents, images |
| **Auth** | NextAuth.js 4 | Authentification |
| **Paiements** | Stripe SDK, PayPal REST | Transactions |
| **Mobile** | Capacitor 7, Java | Apps natives |
| **Email** | Resend + React Email | Notifications |
| **Monitoring** | Prometheus, Grafana | Observabilité |

---

## Modèle de Données

### Diagramme Entité-Relation

```
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│      User       │       │     Booking     │       │      Boat       │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│ id (PK)         │──┐    │ id (PK)         │    ┌──│ id (PK)         │
│ email           │  │    │ userId (FK)     │────┘  │ name            │
│ firstName       │  └───>│ boatId (FK)     │──────>│ capacity        │
│ lastName        │       │ startTime       │       │ status          │
│ phone           │       │ endTime         │       │ lastMaintenance │
│ role            │       │ status          │       └─────────────────┘
│ password        │       │ isPaid          │
│ createdAt       │       │ publicReference │
└─────────────────┘       │ adults          │
         │                │ children        │       ┌─────────────────┐
         │                │ babies          │       │    Payment      │
         │                │ totalAmount     │       ├─────────────────┤
         │                │ language        │    ┌──│ id (PK)         │
         │                │ createdAt       │    │  │ bookingId (FK)  │
         │                └─────────────────┘    │  │ provider        │
         │                        │              │  │ status          │
         │                        │              │  │ amount          │
         │                        └──────────────┘  │ intentId        │
         │                                          │ createdAt       │
         │                                          └─────────────────┘
         │
         │                ┌─────────────────┐       ┌─────────────────┐
         │                │     Block       │       │      Log        │
         │                ├─────────────────┤       ├─────────────────┤
         └───────────────>│ id (PK)         │       │ id (PK)         │
                          │ createdBy (FK)  │       │ userId (FK)     │
                          │ date            │       │ action          │
                          │ reason          │       │ details         │
                          │ scope           │       │ createdAt       │
                          └─────────────────┘       └─────────────────┘
```

### États des Réservations

```
                    ┌─────────────┐
                    │   PENDING   │  ← Réservation créée, paiement en attente
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
              ▼            ▼            ▼
      ┌───────────┐ ┌───────────┐ ┌───────────┐
      │ CONFIRMED │ │ CANCELLED │ │  EXPIRED  │
      │(paiement  │ │(annulé par│ │ (timeout) │
      │ réussi)   │ │ client)   │ │           │
      └─────┬─────┘ └───────────┘ └───────────┘
            │
            │ Jour J
            ▼
      ┌───────────┐
      │ EMBARQUED │  ← Client embarqué (scan QR)
      └─────┬─────┘
            │
    ┌───────┼───────┐
    │               │
    ▼               ▼
┌─────────┐   ┌─────────┐
│COMPLETED│   │ NO_SHOW │
│(succès) │   │(absent) │
└─────────┘   └─────────┘
```

---

## Intégrations

### Stripe

```
┌─────────────────────────────────────────────────────────────────┐
│                      STRIPE INTEGRATION                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────┐    ┌──────────────────────┐           │
│  │   Payment Element    │    │   Terminal SDK       │           │
│  │   (Web Checkout)     │    │   (Tap to Pay)       │           │
│  ├──────────────────────┤    ├──────────────────────┤           │
│  │ • Cards              │    │ • NFC Payment        │           │
│  │ • Apple Pay          │    │ • Reader Connection  │           │
│  │ • Google Pay         │    │ • Mobile Auth        │           │
│  │ • 3D Secure          │    │                      │           │
│  └──────────────────────┘    └──────────────────────┘           │
│              │                          │                        │
│              └────────────┬─────────────┘                        │
│                           │                                      │
│                           ▼                                      │
│                 ┌──────────────────┐                             │
│                 │    Webhooks      │                             │
│                 ├──────────────────┤                             │
│                 │ payment_intent   │                             │
│                 │ .succeeded       │──> Update Booking           │
│                 │ .payment_failed  │──> Cancel Booking           │
│                 │ charge.refunded  │──> Process Refund           │
│                 └──────────────────┘                             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Email Service

```
┌─────────────────────────────────────────────────────────────────┐
│                      EMAIL SYSTEM                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Templates (React Email)          Triggers                       │
│  ┌─────────────────────────┐     ┌─────────────────────────┐    │
│  │ BookingConfirmation.tsx │ <── │ Booking confirmed       │    │
│  │ BookingCancellation.tsx │ <── │ Booking cancelled       │    │
│  │ PaymentReceipt.tsx      │ <── │ Payment received        │    │
│  │ ReviewRequest.tsx       │ <── │ Trip completed (+24h)   │    │
│  │ EmployeeWelcome.tsx     │ <── │ New employee created    │    │
│  └─────────────────────────┘     └─────────────────────────┘    │
│              │                                                   │
│              ▼                                                   │
│  ┌─────────────────────────┐                                    │
│  │      Resend API         │                                    │
│  │   (Delivery Service)    │                                    │
│  └─────────────────────────┘                                    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Sécurité

### Couches de Protection

```
┌─────────────────────────────────────────────────────────────────┐
│                     SECURITY LAYERS                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Layer 1: Network                                                │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ • Cloudflare WAF                                        │    │
│  │ • DDoS Protection                                       │    │
│  │ • SSL/TLS Termination                                   │    │
│  └─────────────────────────────────────────────────────────┘    │
│                           │                                      │
│                           ▼                                      │
│  Layer 2: Application                                            │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ • Rate Limiting (Redis)                                 │    │
│  │ • CORS Whitelist                                        │    │
│  │ • Security Headers (Nginx)                              │    │
│  │ • CSRF Protection (NextAuth)                            │    │
│  └─────────────────────────────────────────────────────────┘    │
│                           │                                      │
│                           ▼                                      │
│  Layer 3: Authentication                                         │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ • JWT Sessions (signed)                                 │    │
│  │ • bcrypt Password Hashing                               │    │
│  │ • Role-Based Access Control                             │    │
│  └─────────────────────────────────────────────────────────┘    │
│                           │                                      │
│                           ▼                                      │
│  Layer 4: Data                                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ • Zod Input Validation                                  │    │
│  │ • Prisma Parameterized Queries                          │    │
│  │ • XSS Sanitization                                      │    │
│  │ • Audit Logging                                         │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Déploiement

### Infrastructure de Production

```
┌─────────────────────────────────────────────────────────────────┐
│                    VPS (OVH Debian 12)                           │
│                     51.178.17.205                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    Docker Network                          │  │
│  │                  (sweetnarcisse-net)                       │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │                                                      │  │  │
│  │  │  ┌────────────┐  ┌────────────┐  ┌────────────┐    │  │  │
│  │  │  │   nginx    │  │  Next.js   │  │ PostgreSQL │    │  │  │
│  │  │  │   :80/443  │─>│   :3000    │─>│   :5432    │    │  │  │
│  │  │  └────────────┘  └────────────┘  └────────────┘    │  │  │
│  │  │                        │               │            │  │  │
│  │  │                        │               ▼            │  │  │
│  │  │                        │        ┌────────────┐      │  │  │
│  │  │                        │        │   Volume   │      │  │  │
│  │  │                        │        │ /var/lib/  │      │  │  │
│  │  │                        │        │ postgresql │      │  │  │
│  │  │                        │        └────────────┘      │  │  │
│  │  │                        │                            │  │  │
│  │  │                        ▼                            │  │  │
│  │  │  ┌────────────┐  ┌────────────┐                    │  │  │
│  │  │  │   MinIO    │  │   Redis    │                    │  │  │
│  │  │  │   :9000    │  │   :6379    │                    │  │  │
│  │  │  └────────────┘  └────────────┘                    │  │  │
│  │  │                                                      │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
│  Monitoring Stack (optional)                                     │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐                 │
│  │ Prometheus │  │Alertmanager│  │  Grafana   │                 │
│  │   :9090    │  │   :9093    │  │   :3001    │                 │
│  └────────────┘  └────────────┘  └────────────┘                 │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Commandes de Déploiement

```bash
# Déploiement initial
docker network create sweetnarcisse-net
docker compose -f docker-compose.db.yml up -d
docker compose up -d --build

# Mise à jour
git pull origin master
docker compose build --no-cache app
docker compose up -d app

# Rollback
docker compose stop app
docker tag sweetnarcisse-app:latest sweetnarcisse-app:failed
docker pull sweetnarcisse-app:previous
docker compose up -d app
```

---

## Métriques et Monitoring

### Endpoints de Santé

| Endpoint | Description |
|----------|-------------|
| `/api/health` | Healthcheck applicatif |
| `/api/metrics` | Métriques Prometheus |

### Métriques Clés

- `http_requests_total` - Total des requêtes HTTP
- `http_request_duration_seconds` - Latence des requêtes
- `booking_created_total` - Réservations créées
- `payment_processed_total` - Paiements traités
- `active_sessions` - Sessions utilisateur actives

---

**Dernière mise à jour** : Décembre 2025  
**Auteur** : Lucas Servais  
**Version** : 1.0.6
